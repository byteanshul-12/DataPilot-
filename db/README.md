# Database - DataPilot

/ PostgreSQL database schema definitions and Drizzle ORM migration configuration.

## Commands

- `npm run db:generate` - Generate SQL migration files from schema
- `npm run db:push` - Push schema updates directly to PostgreSQL
- `npm run db:studio` - Open Drizzle Studio database viewer

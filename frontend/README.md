# Frontend - DataPilot

/ React + Vite + TypeScript web application with Tailwind CSS and Lucide React.

## Commands

- `npm install` - Install application dependencies
- `npm run dev` - Start Vite development server
- `npm run build` - Build production assets
